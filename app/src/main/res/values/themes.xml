<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.MyCookBookRecipeFinderApp" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- ✅ Make Status Bar Transparent -->
        <item name="android:statusBarColor">@android:color/transparent</item>

        <!-- ✅ Allow content to draw under system bars -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- 🚫 Keep status bar icons white (on dark background) -->
        <!-- 'false' means dark icons are NOT forced → white icons show on dark themes -->
        <item name="android:windowLightStatusBar">true</item>

        <!-- 🔁 Do NOT include: android:windowIsTranslucentStatus -->
        <!-- It is deprecated and not supported in Material3 -->

    </style>

    <style name="Theme.MyCookBookRecipeFinderApp" parent="Base.Theme.MyCookBookRecipeFinderApp" />

    <style name="custom_card_view_style" parent="Widget.MaterialComponents.CardView">

        <item name="shapeAppearanceOverlay">@style/custom_card_view</item>
    </style>

    <style name="custom_card_view" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopRight">48dp</item>
        <item name="cornerSizeTopLeft">48dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>
</resources>